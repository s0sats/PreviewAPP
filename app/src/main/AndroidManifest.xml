<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.namoadigital.prj001">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".ui.AppBase"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Login -->
        <activity
            android:name=".ui.act001.Act001_Main"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Customer List -->
        <activity
            android:name=".ui.act002.Act002_Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"></activity>
        <!-- Operation List -->
        <activity
            android:name=".ui.act003.Act003_Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"></activity>

        <receiver android:name=".receiver.WBR_Access" />
        <service android:name=".service.WS_Access" />

        <receiver android:name=".receiver.WBR_GetCustomer" />
        <service android:name=".service.WS_GetCustomer" />

        <receiver android:name=".receiver.WBR_Session" />
        <service android:name=".service.WS_Session" />

        <receiver android:name=".receiver.WBR_Sync" />
        <service android:name=".service.WS_Sync" />

        <receiver android:name=".receiver.WBR_UpdateSoftware" />
        <service android:name=".service.WS_UpdateSoftware" />

    </application>

</manifest>